spring.application.name=FirstSpringProject
spring.datasource.url=jdbc:mysql://localhost:3306/Test_JPA?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=MdAshikur123+
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate DDL Auto (for schema management)
# Options: none, update, create, create-drop, validate
spring.jpa.hibernate.ddl-auto=update

# Show SQL queries in console
spring.jpa.show-sql=true

# Format SQL queries for readability
spring.jpa.properties.hibernate.format_sql=true

# Specify MySQL dialect for Hibernate
#spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
