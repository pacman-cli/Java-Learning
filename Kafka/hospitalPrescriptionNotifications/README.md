# Mini hospital Kafka project has:
- Producer (Doctor) → creates prescriptions. 
- Notification Consumer → notifies patients. 
- Analytics Consumer → logs or processes data for reports.
- JSON object messaging → structured events.