server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/safety_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: safety_user
    password: MdAshikur123+
  jpa:
    hibernate:
      ddl-auto: update   # dev only
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  flyway:
    enabled: false

# JWT config example: in production, set JWT_SECRET via environment variable
app:
  jwt:
    secret: ${JWT_SECRET:ReplaceThisDevSecretKey}   # use env var JWT_SECRET in prod
    expirationMs: 86400000   # 24 hours



