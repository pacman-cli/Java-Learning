# ============================================================================
# Environment Variables Example
# ============================================================================
# Copy this file to .env.local and update with your actual values
# Command: cp .env.example .env.local
#
# NOTE: Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# ============================================================================

# ============================================================================
# REQUIRED VARIABLES
# ============================================================================

# Backend API Base URL
# This should point to your Spring Boot backend API
# Development: http://localhost:8080/api/v1
# Production: https://your-domain.com/api/v1
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# ============================================================================
# OPTIONAL VARIABLES
# ============================================================================

# API Request Timeout (milliseconds)
# Default: 30000 (30 seconds)
# NEXT_PUBLIC_API_TIMEOUT=30000

# Enable Debug Mode
# Shows detailed errors and logs in console
# Default: false
# NEXT_PUBLIC_DEBUG_MODE=false

# Application Name
# Used in page titles and metadata
# NEXT_PUBLIC_APP_NAME=User Management System

# Application Version
# NEXT_PUBLIC_APP_VERSION=1.0.0

# ============================================================================
# ANALYTICS (Optional)
# ============================================================================

# Google Analytics Tracking ID
# NEXT_PUBLIC_GA_TRACKING_ID=

# ============================================================================
# FEATURE FLAGS (Optional)
# ============================================================================

# Enable authentication features
# NEXT_PUBLIC_ENABLE_AUTH=true

# Enable user registration
# NEXT_PUBLIC_ENABLE_REGISTRATION=true

# ============================================================================
# NOTES
# ============================================================================
# 1. Never commit .env.local to version control
# 2. Restart dev server after changing environment variables
# 3. NEXT_PUBLIC_* variables are exposed to the browser (don't use for secrets)
# 4. Non-prefixed variables are only available on the server side
